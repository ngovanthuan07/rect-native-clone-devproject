import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    leftComponentView: {
        flexDirection: 'row'
    },
    leftComponentText: {
        fontWeight: 'bold',
        paddingTop: 2,
        paddingLeft: 10,
    }
})

export default styles;