import { View, Text } from 'react-native'
import React from 'react'
import { Dimensions } from 'react-native'

const {height, width} = Dimensions.get('window')

export default function Input0001() {
  return (
    <View style={{
        flex: 1,
    }}>
      <Text></Text>
    </View>
  )
}